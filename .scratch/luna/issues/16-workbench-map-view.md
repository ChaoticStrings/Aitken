# 16 — Workbench: MapView

**What to build:** A session's GPS track shown on a map, so Vision can connect what
happened to where it happened.

**Blocked by:** 14

**Status:** ready-for-agent

- [ ] Leaflet + OSM map renders the session's GPS track
- [ ] Track is navigable/zoomable alongside the loaded session

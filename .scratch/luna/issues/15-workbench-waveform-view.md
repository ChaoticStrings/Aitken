# 15 — Workbench: WaveformView

**What to build:** The recorded signal and detected segments shown side by side, so
Vision can visually verify the detector is behaving sensibly.

**Blocked by:** 14

**Status:** ready-for-agent

- [ ] Prototype 1's pan/zoom canvas ported over unchanged where possible
- [ ] Segment boundaries overlaid on the waveform, colored by D-scale bucket
- [ ] Handles a full real session (tens of thousands of rows) without unusable lag
